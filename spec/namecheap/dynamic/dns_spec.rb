require 'spec_helper'

RSpec.describe Namecheap::Dynamic::Dns do
  it 'has a version number' do
    expect(Namecheap::Dynamic::Dns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
